server:
  port: 8080
spring:
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        type: trace
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false

master:
  datasource:
    url: jdbc:mysql://10.0.0.122:3306/a?useUnicode=true&character_set_server=utf8mb4&useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: MyNewPass4!
    driverClassName: com.mysql.cj.jdbc.Driver